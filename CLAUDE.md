# Today: to-do app
- Stack: React, TypeScript, Vite. No extra libraries without asking.
- All storage goes through src/storage/adapter.ts, never localStorage directly in components.
- Keep business logic in src/domain as pure functions with tests.
- Match the look of docs/prototype.html (colors, fonts, focus card).
- Run `npm test` and `npm run build` before saying a task is done.
- Small commits, one feature at a time.
